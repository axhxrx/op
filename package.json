{
  "name": "@axhxrx/op",
  "version": "0.9.3",
  "type": "module",
  "exports": "./mod.ts",
  "license": "MIT",
  "scripts": {
    "check": "bun test && bunx tsc --noEmit && deno check mod.ts && deno lint && dprint fmt"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^24.2.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
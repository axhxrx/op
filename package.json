{
  "name": "@axhxrx/op",
  "version": "1.0.0",
  "type": "module",
  "exports": "./mod.ts",
  "license": "MIT",
  "scripts": {
    "check": "bun test && bunx tsc --noEmit && deno check mod.ts && deno lint && dprint fmt"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^24.2.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
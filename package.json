{
  "name": "@axhxrx/op",
  "version": "1.0.1",
  "type": "module",
  "exports": "./mod.ts",
  "license": "MIT",
  "scripts": {
    "check": "deno check && deno lint && bunx tsc --noEmit && dprint check",
    "fix": "dprint fmt",
    "test": "bun test && node --test *.test.ts && deno test -A"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^24.2.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}